import React from 'react';

/**
 * @deprecated This component relies on WorldStateContext which is no longer used.
 * Events are now handled within the new WorldPlayView.
 */
const EventDisplay: React.FC = () => {
  // This component is deprecated - events are handled by WorldPlayView
  return null;
};

export default EventDisplay;