# Vendor Documentation

This directory holds documentation for packages beyond the agent's training cutoff (May 2025).

- **`./`** — Migration guides and breaking-change notes (e.g., when upgrading major versions)
- **`reference/`** — Durable API reference docs that remain useful post-upgrade

Use `bash scripts/agent/read-docs.sh <package>` to surface relevant docs for a given dependency.
